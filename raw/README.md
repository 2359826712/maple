# Raw crawl responses

Raw responses may be written here only when a source explicitly uses `full-text-permitted` and its terms allow storage. Do not commit cookies, tokens, personal data, or full copyrighted articles gathered under another storage mode.

Recommended layout: `raw/{source-id}/{year}/{month}/{content-hash}.{ext}`.
